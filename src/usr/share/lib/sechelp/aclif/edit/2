Select `Y' to test a given combination of user and
group against the entries in the ACL buffer.  The
program computes the way the system would make an
access decision against an object (file or IPC) with
that ACL.
